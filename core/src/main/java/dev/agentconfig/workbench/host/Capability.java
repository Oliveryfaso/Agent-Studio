package dev.agentconfig.workbench.host;

public enum Capability {
    INSTRUCTIONS,
    SCOPED_RULES,
    SKILLS,
    AGENTS,
    POLICIES,
    HOOKS,
    PLUGINS,
    WORKFLOWS,
    EFFECTIVE_CONTEXT,
    SURFACE_AWARE
}
