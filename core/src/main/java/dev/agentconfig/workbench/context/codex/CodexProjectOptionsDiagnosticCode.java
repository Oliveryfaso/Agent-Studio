package dev.agentconfig.workbench.context.codex;

/** Stable machine-readable failures emitted by the bounded configuration parser. */
public enum CodexProjectOptionsDiagnosticCode {
    SNAPSHOT_TOO_LARGE,
    INVALID_UTF8,
    DUPLICATE_TARGET_KEY,
    INVALID_TARGET_VALUE
}
